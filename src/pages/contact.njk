---
layout: page.njk
permalink: /contact/index.html
title: Contact Tilli - Get in Touch with Our Team
description: Contact Tilli for product information, sales inquiries, or support. Reach out to our team via phone, email, or contact form.
keywords: contact, contact us, get in touch, sales, support
hasVtigerIntegration: true
---

    <style>
        /* Modern Flat Dropdown Styles */
        #contact-form select {
            background: white !important;
            border: 2px solid #e5e7eb !important;
            transition: all 0.2s ease !important;
        }
        #contact-form select:hover {
            border-color: #9ca3af !important;
        }
        #contact-form select:focus {
            outline: none !important;
            border-color: #325ef6 !important;
            box-shadow: 0 0 0 3px rgba(91, 158, 255, 0.1) !important;
        }
        #contact-form input[type="text"],
        #contact-form input[type="email"],
        #contact-form input[type="tel"],
        #contact-form textarea {
            border: 2px solid #e5e7eb !important;
            transition: all 0.2s ease !important;
        }
        #contact-form input:hover,
        #contact-form textarea:hover {
            border-color: #9ca3af !important;
        }
        #contact-form input:focus,
        #contact-form textarea:focus {
            outline: none !important;
            border-color: #325ef6 !important;
            box-shadow: 0 0 0 3px rgba(91, 158, 255, 0.1) !important;
        }
    </style>
    <!-- RB2B Tracking -->
    <script type="text/plain" data-cookiecategory="analytics">!function(key) {if (window.reb2b) return;window.reb2b = {loaded: true};var s = document.createElement("script");s.async = true;s.src = "https://b2bjsstore.s3.us-west-2.amazonaws.com/b/" + key + "/" + key + ".js.gz";document.getElementsByTagName("script")[0].parentNode.insertBefore(s, document.getElementsByTagName("script")[0]);}("VN080HX77V6J");</script>
    <!-- Vercel Analytics -->
    <script type="text/plain" data-cookiecategory="analytics" defer src="https://cdn.vercel-insights.com/v1.js"></script>
    <!-- CookieConsent -->
    <style>
        /* Custom CookieConsent Styles */
        .cc-window { font-family: Inter, sans-serif !important; }
        .cc-btn { border-radius: 6px !important; font-weight: 500 !important; }
        .cc-btn.cc-btn-primary { background: #325ef6 !important; border-color: #325ef6 !important; }
        .cc-btn.cc-btn-primary:hover { background: #2a4fd4 !important; }
        .cc-btn.cc-btn-secondary { background: transparent !important; color: #64748b !important; border: 1px solid #e2e8f0 !important; }
        .cc-btn.cc-btn-secondary:hover { background: #f8f9fa !important; }
        #cc-main { font-size: 15px !important; }
        #cc-main .cc-link { color: #325ef6 !important; }
        #cc-main .cc-toggle { background: #e2e8f0 !important; }
        #cc-main .cc-toggle:checked ~ .cc-toggle__slider { background: #325ef6 !important; }
        #cc-main.cc-window.cc-banner { padding: 1.5rem !important; box-shadow: 0 -4px 20px rgba(0,0,0,0.1) !important; }
    </style>
    <main style="margin-top:80px">
        <section class="lead-capture" style="padding-top:40px">
            <div class="container">
                <div class="lead-content" style="align-items:flex-start;gap:28px">
                    <div class="lead-text" style="max-width:520px">
                        <h1 class="page-title" style="font-size:56px;line-height:1.05;margin-bottom:10px">Talk to our Sales Team</h1>
                        <div class="feature-list" style="display:grid;gap:16px;color:#475569">
                            <div>
                                <h3 style="font-size:18px;margin-bottom:6px">Interested in a demo?</h3>
                                <p>Discover how Tilli can streamline your operations, simplify billing, and unlock better payment experiences tailored to your business.</p>
                            </div>
                            <div>
                                <h3 style="font-size:18px;margin-bottom:6px">Trusted by utilities and fintech leaders.</h3>
                                <p>Join companies using Tilli to modernize payments, improve transparency, and stay ahead of customer expectations.</p>
                            </div>
                        </div>
                    </div>

                    <div class="lead-form" style="width:100%;max-width:560px">
                        <form class="contact-form" id="contact-form" onsubmit="handleContactFormSubmit(event)">
                            <div class="form-row">
                                <input type="text" name="firstName" placeholder="First Name" required>
                                <input type="text" name="lastName" placeholder="Last Name" required>
                            </div>

                            <div class="form-row">
                                <input type="email" name="email" placeholder="Email Address" required>
                                <input type="text" name="company" placeholder="Company Name" required>
                            </div>

                            <div class="form-row">
                                <input type="text" name="jobTitle" placeholder="Job Title / Role" required>
                                <select name="country" id="country-select" required style="padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: white; font-size: 1rem; color: #374151; appearance: none; background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27%23374151%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-repeat: no-repeat; background-position: right 0.75rem center; background-size: 20px; padding-right: 2.5rem; transition: border-color 0.2s; cursor: pointer;">
                                    <option value="">Select Country</option>
                                    <option value="US" data-code="+1">United States (+1)</option>
                                    <option value="CA" data-code="+1">Canada (+1)</option>
                                    <option value="GB" data-code="+44">United Kingdom (+44)</option>
                                    <option value="IN" data-code="+91">India (+91)</option>
                                    <option value="AU" data-code="+61">Australia (+61)</option>
                                    <option value="DE" data-code="+49">Germany (+49)</option>
                                    <option value="FR" data-code="+33">France (+33)</option>
                                    <option value="JP" data-code="+81">Japan (+81)</option>
                                    <option value="CN" data-code="+86">China (+86)</option>
                                    <option value="BR" data-code="+55">Brazil (+55)</option>
                                    <option value="MX" data-code="+52">Mexico (+52)</option>
                                    <option value="AE" data-code="+971">UAE (+971)</option>
                                    <option value="SG" data-code="+65">Singapore (+65)</option>
                                    <option value="NL" data-code="+31">Netherlands (+31)</option>
                                    <option value="ES" data-code="+34">Spain (+34)</option>
                                    <option value="IT" data-code="+39">Italy (+39)</option>
                                    <option value="OTHER">Other</option>
                                </select>
                            </div>

                            <div class="form-row">
                                <div style="flex: 1; display: flex; gap: 0.5rem;">
                                    <input type="text"
                                           id="phone-code"
                                           name="phoneCode"
                                           placeholder="+1"
                                           value="+1"
                                           style="width: 80px; padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: #f9fafb; font-size: 1rem; color: #374151; transition: border-color 0.2s;"
                                           readonly>
                                    <input type="tel"
                                           name="phone"
                                           id="phone-number"
                                           placeholder="Phone Number (optional)"
                                           style="flex: 1; padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: white; font-size: 1rem; color: #374151; transition: border-color 0.2s;">
                                </div>
                                <select name="organizationSize" required style="flex: 1; padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: white; font-size: 1rem; color: #374151; appearance: none; background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27%23374151%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-repeat: no-repeat; background-position: right 0.75rem center; background-size: 20px; padding-right: 2.5rem; transition: border-color 0.2s; cursor: pointer;">
                                    <option value="">Organization Size</option>
                                    <option value="1-10">1-10 employees</option>
                                    <option value="11-50">11-50 employees</option>
                                    <option value="51-200">51-200 employees</option>
                                    <option value="201-1000">201-1,000 employees</option>
                                    <option value="1001-5000">1,001-5,000 employees</option>
                                    <option value="5001+">5,001+ employees</option>
                                </select>
                            </div>

                            <div class="form-row">
                                <select name="useCase" required style="flex: 1; padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: white; font-size: 1rem; color: #374151; appearance: none; background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27%23374151%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-repeat: no-repeat; background-position: right 0.75rem center; background-size: 20px; padding-right: 2.5rem; transition: border-color 0.2s; cursor: pointer;">
                                    <option value="">Primary Use Case</option>
                                    <option value="billing-automation">Billing Automation</option>
                                    <option value="payment-processing">Payment Processing</option>
                                    <option value="customer-communications">Customer Communications</option>
                                    <option value="digital-transformation">Digital Transformation</option>
                                    <option value="compliance">Compliance & Security</option>
                                    <option value="cost-reduction">Cost Reduction</option>
                                    <option value="other">Other</option>
                                </select>
                                <select name="monthlyActiveUsers" style="flex: 1; padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: white; font-size: 1rem; color: #374151; appearance: none; background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27%23374151%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-repeat: no-repeat; background-position: right 0.75rem center; background-size: 20px; padding-right: 2.5rem; transition: border-color 0.2s; cursor: pointer;">
                                    <option value="">Monthly Active Users</option>
                                    <option value="<1000">Less than 1,000</option>
                                    <option value="1000-10000">1,000 - 10,000</option>
                                    <option value="10001-50000">10,001 - 50,000</option>
                                    <option value="50001-100000">50,001 - 100,000</option>
                                    <option value="100001-500000">100,001 - 500,000</option>
                                    <option value="500001+">500,000+</option>
                                </select>
                            </div>

                            <select name="transactionVolume" style="width: 100%; padding: 0.75rem; border: 2px solid #e5e7eb; border-radius: 8px; background: white; font-size: 1rem; color: #374151; appearance: none; background-image: url('data:image/svg+xml;charset=UTF-8,%3csvg xmlns=%27http://www.w3.org/2000/svg%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27%23374151%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3e%3cpolyline points=%276 9 12 15 18 9%27%3e%3c/polyline%3e%3c/svg%3e'); background-repeat: no-repeat; background-position: right 0.75rem center; background-size: 20px; padding-right: 2.5rem; transition: border-color 0.2s; cursor: pointer; margin-bottom: 1rem;">
                                <option value="">Expected Annual Transaction Volume</option>
                                <option value="<100000">Less than $100,000</option>
                                <option value="100000-500000">$100,000 - $500,000</option>
                                <option value="500001-1000000">$500,001 - $1,000,000</option>
                                <option value="1000001-5000000">$1,000,001 - $5,000,000</option>
                                <option value="5000001-10000000">$5,000,001 - $10,000,000</option>
                                <option value="10000001-50000000">$10,000,001 - $50,000,000</option>
                                <option value="50000001+">$50,000,000+</option>
                            </select>

                            <textarea name="message" placeholder="Tell us about your specific needs (optional)" rows="3"></textarea>

                            <!-- Math Captcha for Bot Prevention -->
                            <div style="background: #f9fafb; padding: 1rem; border-radius: 8px; margin: 1rem 0;">
                                <label style="display: block; color: #374151; font-weight: 500; margin-bottom: 0.5rem;">
                                    Security Check: What is <span id="math-num1">5</span> + <span id="math-num2">3</span>?
                                    <span style="color: #ef4444;">*</span>
                                </label>
                                <input type="number"
                                       id="math-captcha"
                                       placeholder="Your answer"
                                       style="width: 120px; padding: 0.5rem; border: 2px solid #e5e7eb; border-radius: 6px; font-size: 1rem;"
                                       oninput="validateCaptcha()">
                            </div>

                            <div id="form-status" style="display: none; margin-bottom: 1rem;"></div>

                            <button type="submit" id="submit-btn" class="btn-primary" disabled
                                    style="width: 100%; padding: 0.875rem 1.5rem; background: #325ef6; color: white; border: none; border-radius: 8px; font-size: 1rem; font-weight: 600; cursor: not-allowed; opacity: 0.5; transition: all 0.2s ease;">
                                Request Information
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </section>
    </main>



    <script>
    // Math Captcha variables
    let mathNum1 = 0;
    let mathNum2 = 0;
    let mathAnswer = 0;

    // Generate random math problem
    function generateMathCaptcha() {
        mathNum1 = Math.floor(Math.random() * 10) + 1;
        mathNum2 = Math.floor(Math.random() * 10) + 1;
        mathAnswer = mathNum1 + mathNum2;

        const num1Element = document.getElementById('math-num1');
        const num2Element = document.getElementById('math-num2');
        if (num1Element) num1Element.textContent = mathNum1;
        if (num2Element) num2Element.textContent = mathNum2;

        const captchaInput = document.getElementById('math-captcha');
        if (captchaInput) {
            captchaInput.value = '';
            validateCaptcha();
        }
    }

    // Validate captcha answer and update submit button
    function validateCaptcha() {
        const captchaInput = document.getElementById('math-captcha');
        const submitBtn = document.getElementById('submit-btn');
        const userAnswer = parseInt(captchaInput.value);

        if (userAnswer === mathAnswer) {
            submitBtn.disabled = false;
            submitBtn.style.opacity = '1';
            submitBtn.style.cursor = 'pointer';
            captchaInput.style.borderColor = '#10b981';

            if (!captchaInput.parentElement.querySelector('.captcha-success')) {
                const successIcon = document.createElement('span');
                successIcon.className = 'captcha-success';
                successIcon.innerHTML = ' ✓';
                successIcon.style.color = '#10b981';
                successIcon.style.fontWeight = 'bold';
                captchaInput.parentElement.querySelector('label').appendChild(successIcon);
            }
        } else {
            submitBtn.disabled = true;
            submitBtn.style.opacity = '0.5';
            submitBtn.style.cursor = 'not-allowed';
            captchaInput.style.borderColor = userAnswer && userAnswer !== mathAnswer ? '#ef4444' : '#e5e7eb';

            const successIcon = captchaInput.parentElement.querySelector('.captcha-success');
            if (successIcon) successIcon.remove();
        }
    }

    // Handle Contact Form Submission
    async function handleContactFormSubmit(event) {
        event.preventDefault();

        const form = document.getElementById('contact-form');
        const submitBtn = document.getElementById('submit-btn');
        const statusDiv = document.getElementById('form-status');

        // Verify captcha one more time
        const captchaInput = document.getElementById('math-captcha');
        const userAnswer = parseInt(captchaInput.value);
        if (userAnswer !== mathAnswer) {
            statusDiv.innerHTML = `
                <div style="background: #fee2e2; color: #991b1b; padding: 1rem; border-radius: 8px;">
                    <i class="fas fa-exclamation-circle" style="margin-right: 8px;"></i>
                    Please solve the math problem correctly to continue.
                </div>
            `;
            statusDiv.style.display = 'block';
            return;
        }

        // Get form data
        const formData = new FormData(form);

        // Combine phone code with number
        const phoneCode = formData.get('phoneCode') || '';
        const phoneNumber = formData.get('phone') || '';
        const fullPhone = phoneNumber ? `${phoneCode} ${phoneNumber}` : '';

        const data = {
            firstName: formData.get('firstName'),
            lastName: formData.get('lastName'),
            email: formData.get('email'),
            company: formData.get('company'),
            phone: fullPhone,
            jobTitle: formData.get('jobTitle') || '',
            country: formData.get('country') || '',
            organizationSize: formData.get('organizationSize') || '',
            useCase: formData.get('useCase') || '',
            monthlyActiveUsers: formData.get('monthlyActiveUsers') || '',
            transactionVolume: formData.get('transactionVolume') || '',
            message: formData.get('message') || '',
            formType: 'Contact Form',
            pageUrl: window.location.href,
            submissionDate: new Date().toISOString(),
            source: 'Tilli Website - Contact Form'
        };

        // Disable submit button and show loading state
        submitBtn.disabled = true;
        submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Submitting...';
        statusDiv.style.display = 'none';

        try {
            // Check if running locally for testing
            const isLocalhost = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1';

            let responseData;

            if (isLocalhost) {
                // Simulate successful submission for local testing
                console.log('Local testing mode - Form data:', data);

                // Simulate network delay
                await new Promise(resolve => setTimeout(resolve, 1500));

                responseData = { success: true };

            } else {
                // Production API call
                const apiUrl = 'https://tillisoftware.com/api/vtiger';

                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(data)
                });

                if (!response.ok) {
                    const errorText = await response.text();
                    console.error('API Error Response:', errorText);
                    throw new Error(`API Error: ${response.status}`);
                }

                responseData = await response.json();
            }

            if (responseData.success) {
                statusDiv.innerHTML = `
                    <div style="background: #d1fae5; color: #065f46; padding: 1rem; border-radius: 8px;">
                        <i class="fas fa-check-circle" style="margin-right: 8px;"></i>
                        <strong>Thank you!</strong> We'll contact you within 24 hours.
                    </div>
                `;
                statusDiv.style.display = 'block';

                // Reset form
                form.reset();
                generateMathCaptcha();

                // Track conversion
                if (typeof gtag !== 'undefined') {
                    gtag('event', 'conversion', {
                        'event_category': 'Lead',
                        'event_label': 'Contact Form',
                        'value': 1
                    });
                }
            } else {
                throw new Error('Submission failed');
            }

        } catch (error) {
            console.error('Form submission error:', error);

            statusDiv.innerHTML = `
                <div style="background: #fee2e2; color: #991b1b; padding: 1rem; border-radius: 8px;">
                    <i class="fas fa-exclamation-circle" style="margin-right: 8px;"></i>
                    <strong>Oops!</strong> Something went wrong. Please try again or email us at sales@tilli.pro
                </div>
            `;
            statusDiv.style.display = 'block';
        } finally {
            submitBtn.disabled = false;
            submitBtn.innerHTML = 'Request Information';
        }
    }


    // Initialize form on page load
    document.addEventListener('DOMContentLoaded', function() {
        // Initialize math captcha
        generateMathCaptcha();

        // Disable submit button initially
        const submitBtn = document.getElementById('submit-btn');
        if (submitBtn) {
            submitBtn.disabled = true;
            submitBtn.style.opacity = '0.5';
            submitBtn.style.cursor = 'not-allowed';
        }

        // Add captcha validation listener
        const captchaInput = document.getElementById('math-captcha');
        if (captchaInput) {
            captchaInput.addEventListener('input', validateCaptcha);
            captchaInput.addEventListener('change', validateCaptcha);
        }

        // Handle country selection and phone code
        const countrySelect = document.getElementById('country-select');
        const phoneCodeInput = document.getElementById('phone-code');

        if (countrySelect && phoneCodeInput) {
            countrySelect.addEventListener('change', function() {
                const selectedOption = this.options[this.selectedIndex];
                const countryCode = selectedOption.getAttribute('data-code');

                if (countryCode) {
                    phoneCodeInput.value = countryCode;
                } else if (this.value === 'OTHER') {
                    phoneCodeInput.readOnly = false;
                    phoneCodeInput.value = '';
                    phoneCodeInput.placeholder = 'Code';
                } else {
                    phoneCodeInput.value = '';
                }
            });
        }
    });

    // Legacy code - keeping for compatibility
    (function(){
    })();
    </script>
    <!-- CookieConsent Scripts -->
    <script src="enhanced-cookie-banner.js"></script>
    <script src="script.js"></script>
